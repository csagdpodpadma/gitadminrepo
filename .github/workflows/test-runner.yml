
name: Windows Self-Hosted Runner Fix

on:
  workflow_dispatch: # Allows manual trigger
  push:
    branches: [ main ]

jobs:
  run-on-windows:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ Fix PowerShell Execution Policy
      - name: Set Execution Policy
        run: |
          Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
        shell: powershell

      # ✅ Example command after fixing policy
      - name: Test Runner
        run: |
          echo "✅ Running on self-hosted runner"
        shell: powershell

      # ✅ Optional: Run your actual build/test      # ✅ Optional: Run your actual build/test steps
      - name: Node.js Setup
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        run: npm install

      - name: Run App
