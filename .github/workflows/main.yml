
name: Self-Hosted Runner Demo

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  test:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - name: Print environment info
        run: |
          echo "Hello from self-hosted runner!"
          echo "Current          echo "Current directory: $(pwd)"
          echo "Node version:"
